[package]
name = "wembley-events"
version = "0.1.1"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.11"}
select = { version = "0.5"}
chrono = { version = "0.4"}
icalendar = { version = "0.10"}

[dev-dependencies]
insta = "1.7.2"
